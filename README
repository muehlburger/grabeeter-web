requirements:
 + symfony framework placed in /lib/vendor/symfony
 + lucene part of zend framework placed in /lib/vendor/Zend
 + php5-curl apache module

setup:
 + make sure all requirements are met
 + configure your database connection
